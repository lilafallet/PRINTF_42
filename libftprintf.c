#include <stdlib.h>
#include <stdarg.h>
#include "libft.h"
#include "libftprintf.h"

int	printf(const char *format, ...)
{
	va_list	args;
	int	result;

	va_start(args, format);
	result = /*fonction(format, args) ?*/
	va_end(args);
	return (EXIT_SUCCESS);
}
